#pragma once

#include "Processable.h"
#include "Clearable.h"
#include "Switchable.h"


namespace game
{
	/*class TaskHandler;

	class Process: virtual public Processable, public Clearable, public Switchable
	{
	protected:
		TaskHandler& handler_;

		void detachFromHandler_();

	public:
		Process(TaskHandler& handler);

		virtual void process() abstract override;
		virtual void run() abstract;
		virtual void clear() override;
		virtual void stop() abstract;
		virtual bool hasStopped() const abstract;
	};*/

}

